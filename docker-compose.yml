version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: writer-platform-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: writer
      POSTGRES_PASSWORD: writer123
      POSTGRES_DB: writer_platform
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
